<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.cyf.user.mapper.PermissionMap">

    <resultMap id="baseMap" type="cn.cyf.user.entity.UserInfo">
        <id column="userId" property="id"/>
        <result column="nickname" property="nickname"/>
        <association property="roleInfo" columnPrefix="role_" javaType="cn.cyf.user.entity.RoleInfo">
            <id column="id" property="id"/>
            <result column="name" property="roleName"/>
        </association>
        <collection property="menuList" ofType="string">
            <id column="menuCode" property="menuCode"/>
        </collection>
        <collection property="permissionList" ofType="String">
            <id column="permissionCode" property="permissionCode"/>
        </collection>
    </resultMap>
    <resultMap id="permissionMap" type="cn.cyf.user.result.PermissionInfoResult">
        <id column="menuName" property="menuName"/>
        <collection property="permissions" ofType="cn.cyf.user.result.PermissionInfoResult">
            <id column="id" property="id"/>
            <result column="permissionName" property="permissionName"/>
        </collection>
    </resultMap>

   <select id="getUserPermission" resultMap="baseMap" parameterType="string">
       SELECT
           u.id              userId,
           u.nickname        nickname,
           u.role_id         ,
           r.role_name       ,
           p.menu_code       menuCode,
           p.permission_code permissionCode
       FROM sys_user u
           LEFT JOIN sys_role r ON r.id = u.role_id
           LEFT JOIN sys_role_permission rp ON u.role_id = rp.role_id
           LEFT JOIN sys_permission p ON rp.permission_id = p.id AND rp.delete_status = '1'
       WHERE u.username = #{username}
             AND u.delete_status = '1'
   </select>

    <select id="getAllPermission" resultType="String">
        SELECT p.permission_code permissionCode
        FROM sys_permission p
        ORDER BY p.id
    </select>
    <select id="getAllMenu" resultType="String">
        SELECT p.menu_code menuCode
        FROM sys_permission p
        ORDER BY p.id
    </select>

    <select id="listAllPermission" resultMap="permissionMap">
        SELECT
            p.id                  id,
            p.menu_name           menuName,
            p.permission_name     permissionName,
            p.required_permission requiredPerm
        FROM sys_permission p
    </select>

</mapper>
